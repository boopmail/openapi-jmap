# See https://redocly.com/docs/cli/configuration/

apis:
  jmap@0.1.0:
    # Update the path to your OpenAPI root file
    root: ./json-meta-application-protocol-api.yml

extends:
  - recommended

theme:
  openapi:
    generateCodeSamples:
      languages:
        - lang: curl
        - lang: Node.js
        - lang: Python